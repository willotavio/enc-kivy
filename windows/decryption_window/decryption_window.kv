<DecryptionWindow>:

    orientation: 'vertical'
    pos_hint: {'center_x': 0.5, 'center_y': 0.5}
    size_hint_y: None
    height: 600
    padding: [100,100,100,100]

    TextInput:
        id: text_to_decrypt_input
        hint_text: 'Something to decrypt...'
        padding: [10, (self.height - self.line_height) / 2]
        size_hint_y: None
        height: 50
        password: True

    TextInput:
        id: decryption_key_input
        hint_text: 'Decryption key'
        padding: [10, (self.height - self.line_height) / 2]
        size_hint_y: None
        height: 50
        password: True

    TextInput:
        id: delimiter_input
        hint_text: 'Delimiter (if using, do not use "/" or any existing sequence in the text to be decrypted)'
        padding: [10, (self.height - self.line_height) / 2]
        size_hint_y: None
        height: 50

    Button:
        id: decrypt_button
        text: 'Decrypt'
        size_hint_y: None
        height: 50

    TextInput:
        id: decrypted_result
        size_hint_y: None
        height: 100
        readonly: True
        multiline: True

    Label:
        id: copy_confirmation
        size_hint_y: None
        height: 50

    BoxLayout:
        size_hint_y: None
        orientation: 'horizontal'

        Button:
            text: 'Copy'
            size_hint_y: None
            height: 50
            on_press: root.on_copy_button_press()

        Button:
            text: 'Clear'
            size_hint_y: None
            height: 50
            on_press: decrypted_result.text = ""